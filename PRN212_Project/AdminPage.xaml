<Window x:Class="PRN212_Project.AdminPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PRN212_Project"
        Title="Admin Dashboard - LMS"
        Height="600" Width="1212"
        WindowStartupLocation="CenterScreen"
        Background="#F3F4F6">
    <Window.Resources>
       
        <SolidColorBrush x:Key="BgSidebar" Color="#FF6C8FD9"/>
        <SolidColorBrush x:Key="BgHover" Color="#374151"/>
        <SolidColorBrush x:Key="BgActive" Color="#2563EB"/>
        <SolidColorBrush x:Key="TextLight" Color="#E5E7EB"/>

        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="8,4"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter Margin="6,3"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource BgSidebar}">
            <DockPanel>
              
                <StackPanel DockPanel.Dock="Top" Margin="16,20,0,20">
                    <TextBlock Text="LMS Admin"
                               Foreground="White"
                               FontSize="22"
                               FontWeight="Bold"/>
                    <TextBlock Text="Quản trị hệ thống"
                               Foreground="#9CA3AF"
                               FontSize="12"/>
                </StackPanel>

                <StackPanel>
                    <Button x:Name="BtnUsers"
                            Style="{StaticResource NavButton}"
                            Content="👤  Quản lý User"
                            />

                    <Button x:Name="BtnCourses"
                            Style="{StaticResource NavButton}"
                            Content="📚  Quản lý Khóa học"
                            />
                </StackPanel>

            
            </DockPanel>
        </Border>

       
        <Grid Grid.Column="1" Margin="16">
            <Border Background="White"
                    CornerRadius="10"
                    Padding="20">
                <ContentControl x:Name="ContentHost"/>
            </Border>
        </Grid>
    </Grid>
</Window>
