<UserControl x:Class="PRN212_Project.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PRN212_Project"
             mc:Ignorable="d" 
              d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F9FAFB" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>


        <TextBlock Text="Quản lý người dùng"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="10,0,0,10"/>


        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,10" HorizontalAlignment="Right">


            <Grid>
                <TextBox x:Name="txtSearch"
                         Width="300" Height="32"
                         Margin="0,0,10,0"
                         VerticalContentAlignment="Center"
                         Padding="8"
                         FontSize="13"
                         Background="White"
                         BorderBrush="#D1D5DB"
                         BorderThickness="1"
                         />


            </Grid>

            <Border CornerRadius="6" Background="#EF4444">
                <Button x:Name="btnAdd"
                        Background="Transparent"
                        Foreground="White"
                        FontWeight="Bold"
                        Padding="12,6"
                        BorderThickness="0"
                        Cursor="Hand"
                        Content="👤 Thêm người dùng"/>
            </Border>
        </StackPanel>


        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="10">
            <DataGrid x:Name="dgUsers"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      IsReadOnly="True"
                      BorderThickness="0"
                      RowHeaderWidth="0"
                      AlternatingRowBackground="#F3F4F6"
                      FontSize="13">
                <DataGrid.Columns>


                    <DataGridTextColumn Header="Tên đăng nhập" Binding="{Binding Username}" Width="120"/>


                    <DataGridTextColumn Header="Họ tên" Binding="{Binding FullName}" Width="150"/>


                    <DataGridTextColumn Header="Email"
                    Binding="{Binding Email}"
                    Width="250"
                    MinWidth="200"/>


                    <DataGridTemplateColumn Header="Vai trò" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#3B82F6"
                                           CornerRadius="15"
                                              Padding="6,1"
                                                MinWidth="60"
                                              HorizontalAlignment="Center"
                                         VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Role}"
                                      Foreground="White"
                         FontWeight="Bold"
                              FontSize="11"
                             HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                                TextAlignment="Center"/>
                                </Border>

                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>



                    
                    <DataGridTemplateColumn Header="Thao tác" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="✏️" Width="28" Height="28" Margin="2" Padding="0" Background="Transparent" BorderThickness="0"/>
                                    <Button Content="🗑️" Width="28" Height="28" Margin="2" Padding="0" Background="Transparent" BorderThickness="0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
